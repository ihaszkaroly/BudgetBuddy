{
  "private": true,
  "name": "budgetbuddy",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "npm run build:js && vite", 
    "build:js": "dotnet fable src/App.fsproj --outDir src",
    "build": "npm run build:js && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^6.3.5"
  }
}
